{% extends 'layout_backend.html.twig' %}


{% block title %}Base Layout{% endblock %}

{% block content_header_username %}

{% endblock %}


{# Navbar Side + active Link #}
{% block navbar %}
    {{ parent() }}
    {% block navActiveBlog %}class="active"{% endblock %}
{% endblock %}


{# Content-Header, hier Titel und optionalen Text setzen #}
    {% block pageHeader %}
        {{ parent() }}
        {% block pageHeaderDescription %}ein Text{% endblock %}
        {% block pageHeaderTitel %}Blog{% endblock %}
    {% endblock %}


{% block content %}

        <!-- Main content -->
        <section class="content">

            <!-- Your Page Content Here -->
            {# Leist mit defaul actions #}

                <div class="btn-group btn-group-justified">
                    <a class="btn btn-info" href="{{ path('backend_blog_new') }}">Neuer Artikel</a>
                    <a class="btn btn-default" href="{{ path('backend_blog_seo') }}">Seo Einstellungen</a>
                </div>

            <br><br><br>


            <table id="blogTable" class="table table-striped">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Titel</th>
                    <th>actions</th>
                </tr>
                </thead>

                <tbody>
                {% for items in data %}
                    <tr>
                        <td>{{ items.id }}</td>
                        <td>{{ items.titel }}</td>
                        <td>
                            <button data-tooltip="Beitrag nach oben verschieben" data-tooltip-position="top"><span class="glyphicon glyphicon-arrow-up" aria-hidden="true"></span></button>
                            <button data-tooltip="Beitrag nach unten verschieben" data-tooltip-position="top"><span class="glyphicon glyphicon-arrow-down" aria-hidden="true"></span></button>
                            <button data-tooltip="Beitrag bearbeiten" data-tooltip-position="top"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></button>
                            <button data-tooltip="Beitrag löschen" data-tooltip-position="top"><span class="glyphicon glyphicon-remove" aria-hidden="true"></span></button>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

        </section><!-- /.content -->

{% endblock %}